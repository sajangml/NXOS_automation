[defaults]
inventory = ./inventory/hosts.yml
host_key_checking = False
retry_files_enabled = False
gathering = explicit
roles_path = ./roles

[persistent_connection]
command_timeout = 60
connect_timeout = 60
